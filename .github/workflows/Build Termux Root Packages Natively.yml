name: Build Unix Root Packages Natively

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - dev
    paths:
      - 'root-packages/**'
      - 'scripts/**'

jobs:
  build-root-packages:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        arch: [aarch64, arm, i686, x86_64]
      fail-fast: false

    steps:
      # Step 1: Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      # Step 2: Ensure essential utilities are installed
      - name: Ensure essential tools are available
        run: |
          sudo apt-get update -y
          sudo apt-get install -y jq curl unzip xz-utils lzip git

      # Step 3: Verify Python availability
      - name: Check Python version
        run: |
          python3 --version
          pip3 --version

      # Step 4: Setup Unix-native build environment
      - name: Setup Unix Build Environment
        run: |
          set -e
          set -o pipefail
          chmod +x ./scripts/utils/setup-native-unix.sh
          ./scripts/utils/setup-native-unix.sh 2>&1 | tee /tmp/setup-native-unix.log

      # Step 5: Generate per-package build.sh files using Python
      - name: Generate build.sh files
        run: |
          chmod +x ./scripts/utils/Unix_packages.sh
          python3 ./scripts/utils/Unix_packages.sh 2>&1 | tee /tmp/Unix_packages.sh.log

      # Step 6: Build only root-packages for the target architecture
      - name: Build root-packages for ${{ matrix.arch }}
        run: |
          chmod +x ./build.sh
          ./build.sh $(ls root-packages) 2>&1 | tee /tmp/unix-build-${{ matrix.arch }}.log

      # Step 7: Archive built .deb files (optional: future packaging)
      - name: Archive built .deb files
        run: |
          mkdir -p artifacts
          find output -type f -name '*.deb' -exec mv {} artifacts/ \; || true

      # Step 8: Upload built package artifacts
      - name: Upload built packages
        uses: actions/upload-artifact@v7
        with:
          name: unix-root-debs-${{ matrix.arch }}
          path: artifacts/

      # Step 9: Upload setup/build logs for debugging
      - name: Upload setup/build logs
        if: failure() || always()
        uses: actions/upload-artifact@v7
        with:
          name: unix-build-logs-${{ matrix.arch }}
          path: |
            /tmp/setup-native-unix.log
            /tmp/Unix_packages.sh.log
            /tmp/unix-build-${{ matrix.arch }}.log
